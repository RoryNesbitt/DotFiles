export SHCOLOUR="162"
export ECHOCOLOUR="1;31"
if [ -f $ZDOTDIR/device/$(hostname).env ]; then
  source $ZDOTDIR/device/$(hostname).env
fi
export RGB_SHCOLOUR="$($HOME/.local/scripts/colours $SHCOLOUR)"
